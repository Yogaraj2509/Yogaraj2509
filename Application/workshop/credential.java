public class credential {
    //the purpose of this credeential class is to store the signup information
    //we will treat this as database

    static int db_sid;
    static int db_pwd;
    static long db_contact;
    static int db_age;
    static boolean did_signup_done;
    static int log_in_attempt=0;
    /*
     * did sign up is bollean where true means yes false means no
     * yes means old user (did_signup_done=true)
     * no means new user(did_signup_done=false)
     */
}
